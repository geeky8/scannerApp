/// [FileState] for managing states of File.
// ignore_for_file: constant_identifier_names

enum FileState {
  ///[FileState.LOADING] change in UI when selecting orders.
  SELECTED,

  ///[FileState.SUCCESS] normal state.
  NORMAL,
}
